
enum SecondsLamp {
    case on
    case off
}

enum FiveHourLamp {
    case redColor
    case noColor
}

enum OneHourLamp {
    case redColor
    case noColor
}

enum FiveMinuteLamp {
    case redColor
    case yellowColor
    case noColor
}

enum OneMinuteLamp {
    case yellowColor
    case noColor
}
